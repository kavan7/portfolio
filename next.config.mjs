/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com', 'rajgilllaw.com', 'pbs.twimg.com', 'images.unsplash.com', 'upload.wikimedia.org', 'pll.harvard.edu'],
      
    },
  };
  
  export default nextConfig;